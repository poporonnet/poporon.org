---
interface Props {
  name: string;
  icon: string;
}

const { name, icon } = Astro.props;
---

<div class="flex items-center gap-1">
  <img src={icon} class="size-4 rounded-full border border-gray-300" />
  <p class="font-sm">{name}</p>
</div>
